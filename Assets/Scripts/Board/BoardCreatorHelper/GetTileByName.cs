using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace MM26.Board.Helper
{
    /// <summary>
    /// Populates Tile database in gameobject so they don't need to be
    /// fetched from the filepath on startup.
    /// Only meant to be used in the UnityEditor. Do not use in actual build.
    /// </summary>
    public class GetTileByName : MonoBehaviour
    {

        // Start is called before the first frame update
        void Start()
        {

        }

        // Update is called once per frame
        void Update()
        {

        }
    }
}
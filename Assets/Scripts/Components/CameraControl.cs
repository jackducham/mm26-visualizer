using Unity.Entities;

namespace MM26.Components
{
    /// <summary>
    /// When this component is present, the camera is controlled by
    /// the user
    /// </summary>
    [GenerateAuthoringComponent]
    public struct CameraControl : IComponentData
    {
    }
}
